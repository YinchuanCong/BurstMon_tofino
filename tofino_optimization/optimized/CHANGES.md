# Reproducing the Optimized Tofino Build

Run the repository-level workflow from the repository root:

```bash
./run_tofino_10us_32bit.sh
```

Show the available configuration parameters:

```bash
./run_tofino_10us_32bit.sh --help
```
